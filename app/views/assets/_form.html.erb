<%= simple_form_for asset, :html => {:multipart => true}  do |form| %>
    <%= form.input :name %>
    <%= form.input :file, :as => :file %>
    <div class="actions">
      <%= button_submit_tag (asset.new_record? ? 'Subir fichero' : 'Actualizar fichero') %>
      <%= link_to 'Cancelar', (asset.new_record? ? assets_path : [asset]) %>
    </div>
<% end %>